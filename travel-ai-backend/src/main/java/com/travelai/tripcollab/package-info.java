/**
 * Trip collaboration module — invite travel companions to a trip (viewer/editor
 * roles) and vote on itinerary stops. A "trip" is keyed by the booking id, the
 * same id used by the /trips pages and the live itinerary.
 */
@org.springframework.modulith.ApplicationModule
package com.travelai.tripcollab;
