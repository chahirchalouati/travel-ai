package com.travelai.travel.budget;

/** Category of a manual out-of-pocket expense logged against a trip. */
public enum TripExpenseCategory {
    FOOD,
    TRANSPORT,
    SHOPPING,
    ACTIVITIES,
    OTHER
}
